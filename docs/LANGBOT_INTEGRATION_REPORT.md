# TrendRadar + LangBot 集成研究报告 (修订版)

> 版本: 2.0
> 更新: 2026-02-02
> 重点: LangBot 内部机制与接口设计

## 一、LangBot 核心能力矩阵

### 1.1 组件类型与用途

| 组件类型 | 描述 | TrendRadar 应用场景 |
|----------|------|---------------------|
| **Command** | 用户显式调用的命令 | `!tr` 系列命令 |
| **Tool** | LLM 自动调用的工具 | AI 分析时调用搜索/数据接口 |
| **EventListener** | 监听系统事件 | 消息处理、定时触发、链接解析 |
| **KnowledgeRetriever** | 知识库检索器 | 历史新闻检索、RAG 增强 |

### 1.2 可利用的现有插件

```
┌─────────────────────────────────────────────────────────────────────┐
│                    LangBot 插件生态 (TrendRadar 可用)                │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ 信息获取层                                                   │   │
│  │   WebSearch ────────── LLM 联网搜索（补充分析上下文）        │   │
│  │   LinkAnaly ────────── 链接解析（用户分享的新闻链接）        │   │
│  │   RAGFlowRetriever ─── 知识库检索（历史新闻/分析报告）       │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                               │                                      │
│                               ▼                                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ 任务调度层                                                   │   │
│  │   TaskTimer ────────── 定时触发脚本（替代外部cron）          │   │
│  │   ScheNotify ───────── 自然语言日程（"每天9点推送"）         │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                               │                                      │
│                               ▼                                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ 输出处理层                                                   │   │
│  │   Markdowm2ing_Pro ─── AI回复转图片（分析报告美化）          │   │
│  │   Text2MsgChain ────── 结构化文本转消息链（图文混排）        │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                               │                                      │
│                               ▼                                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ 状态管理层                                                   │   │
│  │   ContextKeeper ────── 上下文持久化（用户偏好、订阅状态）    │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 二、LangBot 接口层设计

### 2.1 消息推送接口

#### 方案 A: HTTP API (推荐)

```
TrendRadar Daemon ──HTTP POST──▶ LangBot API ──▶ 飞书/钉钉/...
```

**优点**:
- 解耦，TrendRadar 不依赖 LangBot 内部实现
- 支持多实例部署
- 可以在 TrendRadar 侧做重试/限流

**接口设计** (基于 LangBot HTTP API):
```python
# TrendRadar 推送客户端
class LangBotPushClient:
    def __init__(self, api_url: str, api_key: str):
        self.api_url = api_url
        self.api_key = api_key

    async def push_message(self, target: PushTarget, content: PushContent):
        """
        推送消息到 LangBot

        target: 推送目标 (群聊ID/用户ID)
        content: 推送内容 (支持文本/Markdown/卡片)
        """
        pass
```

#### 方案 B: 推送队列 (当前方案)

```
TrendRadar Daemon ──JSON文件──▶ Push Queue ──▶ feishu_push ──▶ 飞书
```

**优点**: 简单可靠
**缺点**: 不经过 LangBot，无法利用插件能力

#### 方案 C: 混合方案 (推荐最终方案)

```
                    ┌─────────────────────────────────────┐
                    │         TrendRadar Daemon           │
                    └──────────────┬──────────────────────┘
                                   │
                    ┌──────────────┴──────────────┐
                    ▼                              ▼
            ┌──────────────┐              ┌──────────────┐
            │ 即时推送      │              │ 增强推送     │
            │ (Push Queue)  │              │ (LangBot API)│
            └──────┬───────┘              └──────┬───────┘
                   │                              │
                   ▼                              ▼
            ┌──────────────┐              ┌──────────────┐
            │ 简单文本消息  │              │ 经过插件处理  │
            │ 低延迟        │              │ Markdown转图 │
            │              │              │ 知识库关联    │
            └──────────────┘              └──────────────┘
```

### 2.2 消息内容模型

```python
# trendradar/models/push_message.py

from dataclasses import dataclass
from enum import Enum
from typing import Optional, List, Dict, Any

class MessageType(Enum):
    RAW_NEWS = "raw_news"           # 原始新闻
    AI_ANALYSIS = "ai_analysis"     # AI 分析报告
    DAILY_REPORT = "daily_report"   # 日报
    SIGNAL = "signal"               # 交易信号
    ALERT = "alert"                 # 告警

class ContentFormat(Enum):
    TEXT = "text"
    MARKDOWN = "markdown"
    CARD = "card"                   # 飞书卡片
    IMAGE = "image"

@dataclass
class PushTarget:
    """推送目标"""
    channel: str                    # feishu/dingtalk/telegram/email
    target_type: str                # group/user
    target_id: str                  # 群聊ID/用户ID

@dataclass
class PushContent:
    """推送内容"""
    format: ContentFormat
    title: str
    body: str
    metadata: Dict[str, Any]        # 扩展字段

@dataclass
class PushMessage:
    """统一推送消息模型"""
    id: str
    type: MessageType
    content: PushContent
    targets: List[PushTarget]
    priority: int = 0               # 0=普通, 1=重要, 2=紧急
    timestamp: str = ""
    source: str = "trendradar"

    # 扩展字段
    news_items: Optional[List] = None
    analysis_result: Optional[Dict] = None
    related_symbols: Optional[List[str]] = None  # 相关股票代码
```

---

## 三、插件集成方案

### 3.1 TaskTimer 集成 (定时推送)

**目标**: 用 TaskTimer 替代外部 cron，实现定时推送

```yaml
# TaskTimer 配置示例
tasks:
  - name: morning_news
    cron: "0 9 * * *"           # 每天早上9点
    script: |
      # 触发 TrendRadar 推送
      curl -X POST http://trendradar:8080/api/push/daily

  - name: market_close
    cron: "0 15 * * 1-5"        # 周一到周五下午3点
    script: |
      # 触发收盘分析
      curl -X POST http://trendradar:8080/api/analyze/daily_summary
```

**TrendRadar 侧需要**:
- 添加 HTTP API 接口 (`/api/push/daily`, `/api/analyze/...`)
- 或者直接调用内部函数

### 3.2 WebSearch 集成 (联网增强)

**目标**: AI 分析时自动调用 WebSearch 补充信息

```
新闻输入 ──▶ 实体提取 ──▶ WebSearch 查询 ──▶ 上下文增强 ──▶ AI 分析
              │              │
              ▼              ▼
         [公司名称]    [相关新闻/研报]
         [股票代码]    [最新财务数据]
```

**实现方式**:
1. **方式一**: 在 LangBot 对话中，LLM 自动决定是否调用 WebSearch
2. **方式二**: TrendRadar 分析流程中显式调用

### 3.3 RAGFlowRetriever 集成 (历史知识)

**目标**: 实现 ROADMAP 中的"历史信息分析"

```
┌─────────────────────────────────────────────────────────────────────┐
│                    RAGFlow 知识库架构                                │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  数据入库 (TrendRadar 侧)                                           │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ 新闻 ──▶ AI 分析 ──▶ 结构化存储 ──▶ RAGFlow 知识库          │   │
│  │                         │                                    │   │
│  │                         ▼                                    │   │
│  │              [标题/摘要/分析/股票/行业/情感]                 │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                      │
│  知识检索 (LangBot 侧)                                              │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │ 用户提问 ──▶ RAGFlowRetriever ──▶ 相似新闻/分析 ──▶ 回答   │   │
│  │                                                              │   │
│  │ 示例:                                                        │   │
│  │ Q: "上次央行降准后市场反应如何？"                           │   │
│  │ A: [检索历史新闻] + [历史分析报告] + [当时市场走势]          │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                      │
└─────────────────────────────────────────────────────────────────────┘
```

### 3.4 Markdowm2ing_Pro 集成 (美化输出)

**目标**: AI 分析报告以图片形式发送

**当前流程**:
```
AI 分析 ──▶ 文本格式 ──▶ 飞书消息
```

**增强流程**:
```
AI 分析 ──▶ Markdown 格式 ──▶ Markdowm2ing_Pro ──▶ 精美图片 ──▶ 飞书消息
```

**优势**:
- 复杂表格/数据更易读
- 代码高亮
- 专业报告风格

---

## 四、trendradar 插件重构方案

### 4.1 代码优化

```python
# 优化后的结构
docker/langbot_data/plugins/trendradar/
├── manifest.yaml
├── main.py                    # 插件主类
├── components/
│   └── commands/
│       ├── tr.py              # 命令组件
│       └── tr.yaml            # 命令配置
├── services/                  # 新增: 业务逻辑
│   ├── config_reader.py       # 配置读取服务
│   ├── status_checker.py      # 状态检查服务
│   └── push_client.py         # 推送客户端
├── models/                    # 新增: 数据模型
│   └── responses.py           # 响应模型
└── utils/                     # 新增: 工具函数
    ├── formatters.py          # 格式化工具
    └── constants.py           # 常量定义
```

### 4.2 消除重复代码

```python
# 优化前: 帮助文本重复
# tr.py:28-36 和 tr.py:189-197

# 优化后: 提取常量
# constants.py
HELP_TEXT = """📚 TrendRadar 配置查看命令

可用命令:
• !tr help - 显示此帮助信息
• !tr keywords - 显示关键词过滤配置
• !tr prompt - 显示 AI 分析提示词
• !tr status - 显示推送队列状态

输入 !tr help 查看详细帮助"""
```

### 4.3 添加日志

```python
# 优化后: 添加结构化日志
import logging

logger = logging.getLogger("trendradar.plugin")

async def cmd_keywords(self, ctx: ExecuteContext):
    logger.info(f"用户 {ctx.sender_id} 请求查看关键词配置")
    try:
        content = self._read_keywords_config()
        logger.debug(f"成功读取关键词配置: {len(content)} 字符")
        yield CommandReturn(text=content)
    except Exception as e:
        logger.error(f"读取关键词配置失败: {e}", exc_info=True)
        yield CommandReturn(text=f"❌ 读取失败: {e}")
```

---

## 五、开发路线图 (修订版)

### Phase 1: 基础优化 (当前)

| 任务 | 优先级 | 依赖插件 | 说明 |
|------|--------|----------|------|
| 重构 trendradar 插件代码 | P0 | - | 消除重复、添加日志 |
| 定义 PushMessage 数据模型 | P0 | - | 统一推送内容格式 |
| 安装基础插件 | P0 | ContextKeeper, Markdowm2ing_Pro | 解决重启失忆、美化输出 |

### Phase 2: 定时与搜索

| 任务 | 优先级 | 依赖插件 | 说明 |
|------|--------|----------|------|
| 集成 TaskTimer | P1 | TaskTimer | 定时推送替代 cron |
| 集成 WebSearch | P1 | WebSearch | AI 分析联网增强 |
| 添加 TrendRadar HTTP API | P1 | - | 供 TaskTimer 调用 |

### Phase 3: 知识库与交互

| 任务 | 优先级 | 依赖插件 | 说明 |
|------|--------|----------|------|
| 部署 RAGFlow | P2 | RAGFlowRetriever | 历史新闻存储 |
| 实现订阅功能 | P2 | ContextKeeper | 用户订阅关键词 |
| 集成 LinkAnaly | P2 | LinkAnaly | 用户分享链接分析 |

### Phase 4: 高级功能

| 任务 | 优先级 | 依赖插件 | 说明 |
|------|--------|----------|------|
| ScheNotify 集成 | P3 | ScheNotify | 自然语言日程 |
| 交易信号生成 | P3 | - | 从分析中提取信号 |
| 多 Pipeline 配置 | P3 | - | 不同场景不同处理 |

---

## 六、架构总览图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        TrendRadar + LangBot 集成架构 (目标)                   │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                      TrendRadar Core                                 │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────────┐  │   │
│  │  │ Crawler     │  │ AI Analyzer │  │ PushMessage Builder         │  │   │
│  │  │ (同花顺等)   │─▶│ (CrewAI)    │─▶│ (统一推送模型)              │  │   │
│  │  └─────────────┘  └─────────────┘  └──────────────┬──────────────┘  │   │
│  └───────────────────────────────────────────────────┼──────────────────┘   │
│                                                      │                      │
│                      ┌───────────────────────────────┴───────────────────┐  │
│                      │                                                    │  │
│                      ▼                                                    ▼  │
│  ┌─────────────────────────────────┐      ┌─────────────────────────────┐  │
│  │ 即时推送 (低延迟)                │      │ 增强推送 (LangBot Pipeline) │  │
│  │ ┌─────────────────────────────┐│      │ ┌─────────────────────────┐ │  │
│  │ │ Push Queue → feishu_push   ││      │ │ LangBot HTTP API        │ │  │
│  │ └─────────────────────────────┘│      │ └───────────┬─────────────┘ │  │
│  └─────────────────────────────────┘      │             │               │  │
│                                            │             ▼               │  │
│                                            │ ┌─────────────────────────┐ │  │
│                                            │ │ 插件处理链               │ │  │
│                                            │ │ WebSearch → 补充信息    │ │  │
│                                            │ │ RAGFlow → 历史关联      │ │  │
│                                            │ │ Markdown2Img → 美化    │ │  │
│                                            │ └───────────┬─────────────┘ │  │
│                                            └─────────────┼───────────────┘  │
│                                                          │                  │
│  ┌───────────────────────────────────────────────────────┴──────────────┐  │
│  │                        LangBot 核心服务                               │  │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────────────┐   │  │
│  │  │ Pipeline    │  │ Bot Manager │  │ Platform Adapters           │   │  │
│  │  │ 管理器      │  │ 机器人管理   │  │ 飞书/钉钉/TG/...            │   │  │
│  │  └─────────────┘  └─────────────┘  └─────────────────────────────┘   │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                        后台任务 (TaskTimer)                           │  │
│  │  ┌─────────────────────────────────────────────────────────────┐    │  │
│  │  │ 定时任务                                                     │    │  │
│  │  │ • 9:00 每日早报                                             │    │  │
│  │  │ • 15:00 收盘分析                                            │    │  │
│  │  │ • */30 * * * * 新闻增量检查                                 │    │  │
│  │  └─────────────────────────────────────────────────────────────┘    │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
│  ┌──────────────────────────────────────────────────────────────────────┐  │
│  │                        知识库 (RAGFlow)                               │  │
│  │  ┌─────────────────────────────────────────────────────────────┐    │  │
│  │  │ • 历史新闻存储                                               │    │  │
│  │  │ • AI 分析报告存档                                           │    │  │
│  │  │ • 相似新闻检索                                               │    │  │
│  │  │ • 用户偏好持久化 (ContextKeeper)                            │    │  │
│  │  └─────────────────────────────────────────────────────────────┘    │  │
│  └──────────────────────────────────────────────────────────────────────┘  │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 七、下一步行动

### 立即可做 (今天)

1. **安装基础插件**
   - ContextKeeper (上下文持久化)
   - Markdowm2ing_Pro (输出美化)

2. **重构 trendradar 插件**
   - 消除重复代码
   - 添加日志
   - 提取常量

### 短期 (本周)

3. **安装功能插件**
   - TaskTimer (定时任务)
   - WebSearch (联网搜索)

4. **添加 TrendRadar HTTP API**
   - `/api/push/trigger` - 手动触发推送
   - `/api/status` - 服务状态

### 中期 (下周)

5. **配置 Pipeline**
   - 创建财经分析专用 Pipeline
   - 启用相关插件

6. **评估 RAGFlow**
   - 确认是否需要部署
   - 规划知识库结构

---

*文档维护: Claude Code*
*版本: 2.0*
